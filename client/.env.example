VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Leave empty for local dev (Vite proxy handles /api/* → localhost:4000)
# Set to https://inventory-4zxb.onrender.com in Netlify env vars for production
VITE_API_URL=
