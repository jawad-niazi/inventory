Multi-Tenant Inventory Management (Boilerplate)

This repository contains the skeleton for a Multi-Tenant Inventory Management System.

Structure:

- `client/` — React + Vite frontend
- `server/` — Node.js + Express backend

This commit only creates the project structure and minimal boilerplate files. No features are implemented yet.

Next steps:

- Install dependencies in `client/` and `server/`.
- Implement features and connect to Supabase.
